services:

  greenroots-db:
    container_name: greenroots-db
    image: postgres:latest
    ports:
      - "5433:5432"
    restart: unless-stopped
    env_file:
      - ./docker.env
    volumes:
      - ./data/init.sql:/docker-entrypoint-initdb.d/init.sql
      - ./data/seeding.sql:/docker-entrypoint-initdb.d/seeding.sql
      - ./pg-greenroots:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U greenroots -d greenroots"]
      start_period: 15s
      interval: 15s
      timeout: 5s
      retries: 5

  greenroots-backend:
    container_name: greenroots-backend
    build:
      context: ./backend 
      dockerfile: Dockerfile 
    ports:
      - "3000:3000"
    restart: always
    env_file:
      - ./docker.env
    volumes:
      - ./backend:/app/
    depends_on:
      greenroots-db:
        condition: service_healthy

  greenroots-backoffice:
    container_name: greenroots-backoffice
    build:
      context: ./backoffice
      dockerfile: Dockerfile 
    ports:
      - "3001:3000"
    restart: always
    env_file:
      - ./docker.env
    volumes:
      - ./backoffice:/app/
    depends_on: 
      - greenroots-backend